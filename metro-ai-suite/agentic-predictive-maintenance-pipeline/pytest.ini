# Top-level pytest config — runs all service tests via Makefile (make test).
# To run a single service: cd services/<service> && pytest
[pytest]
collect_ignore_glob = ["services/*/src/*", "services/*/tests/*"]
